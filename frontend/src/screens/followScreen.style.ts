import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    containerCalendar: {
        width: "100%",
        flex: 1,
        alignItems: "center",
    },
    calendar: {
        width: 300,
    },
});

export default styles;
